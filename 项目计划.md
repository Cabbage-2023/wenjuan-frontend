# 问卷系统全端同步 + GitHub Actions 部署方案

## 背景

问卷系统有 B端（前端）、C端（Next.js答题页）、后端（NestJS）三个项目。开发/部署环境分散：

- **本地 Windows**：日常开发，各自有 git 仓库
- **服务器 192.144.232.63**：通过 `docker compose` 运行，有时直接在服务器上改代码
- **GitHub**：三个远端仓库（`wenjuan-frontend`、`wenjuan-client`、`wenjuan-backend`）

## 架构完整流量路径

```
用户浏览器 → http://natsumeai.icu (port 80)
  ↓ natapp 内网穿透
宿主机 nginx (port 80) — /etc/nginx/sites-available/wenjuan
  │
  ├── /                          → B端 Docker nginx:3001 → SPA (index.html)
  ├── /question/edit/*           → B端 Docker nginx:3001 ✅ (修复刷新404)
  ├── /question/stat/*           → B端 Docker nginx:3001 ✅ (修复刷新404)
  ├── /question/*                → C端 Next.js:3000 (答题页)
  ├── /_next/*                   → C端 Next.js:3000 (静态资源)
  ├── /api/*                     → 后端 NestJS:3005
  └── /ai/*                      → AI 项目 Next.js:3039
```

**B端 Docker 内部 nginx (port 3001)：**
```
/     → /usr/share/nginx/html/index.html (SPA, try_files回退)
/api/ → proxy_pass http://backend:3005 (Docker内网)
```

## 三端配置一致性校验 ✅

### 端口映射一致性 ✅
| 层 | B端 | C端 | 后端 |
|----|-----|-----|------|
| docker-compose | 3001:3001, 3002:3002 | 3000:3000 | 3005:3005 |
| 宿主机 nginx | 127.0.0.1:3001 | 127.0.0.1:3000 | 127.0.0.1:3005 |
| 容器内部 | nginx listen 3001 | Next.js 3000 | NestJS 3005 |
| **结论** | ✅ 一致 | ✅ 一致 | ✅ 一致 |

### Docker 内网名称一致性 ✅
| 调用方 | 目标 | 地址 | 是否匹配 docker-compose service名 |
|--------|------|------|----------------------------------|
| B端 nginx (`nginx.conf`) | 后端API | `http://backend:3005` | ✅ service名就是 `backend` |
| C端 Next.js (`.env`) | 后端API | `http://backend:3005` | ✅ 同上 |
| 后端 NestJS (`.env`) | MongoDB | `mongo:27017` | ✅ service名就是 `mongo` |

### Dockerfile 一致性 ✅
| 项目 | 基础镜像 | 构建方式 | 端口 |
|------|---------|---------|------|
| B端 | `node:20-alpine` → `nginx:alpine` | 多阶段构建 | EXPOSE 3001 3002 |
| C端 | `node:20-alpine` | 单阶段，`npm run build` → `npm start` | EXPOSE 3000 |
| 后端 | `node:20-alpine` | 单阶段，`npm run build` → `npm run start:prod` | EXPOSE 3005 |

### nginx SPA 回退配置 ✅
B端 `nginx.conf` 已有 `try_files $uri $uri/ /index.html;`，SPA路由刷新正常。

### B端 nginx 本地 vs 服务器对比 ✅
本地 `nginx.conf` 与服务器上 Docker 内的一致，无需修改。

### 潜在冲突检查 ✅
- 端口无重叠 (3000/3001/3002/3005 各不相同)
- Docker内网 service 名不冲突
- Host nginx location 块无歧义匹配（正则 `~ ^/question/(edit|stat)/` 优先级高于普通前缀 `location /question/`）
- `.env` 文件无硬编码 IP（都使用 Docker 内网 service 名）

问题：服务器代码和本地代码不同步，有些修改没提交，有些提交没推送，部署靠手动。需要把一切理顺，让 `git push` 自动触发部署。

## 现状

| 项目 | 本地 remote | 服务器 remote | 同步状态 |
|------|------------|--------------|---------|
| **B端** wenjuan-frontend | `wenjuan-fe.git` → 已改名 `wenjuan-frontend.git` | `wenjuan-frontend.git` ✅ | 本地多 1 commit (`7805ea9`)，服务器有 3 个未提交修改 |
| **C端** wenjuan-client | `wenjuan-client.git` ✅ | `wenjuan-client.git` ✅ | 本地多 1 commit (`f3358dc`)，服务器有 1 个未提交（README+脚本，不重要） |
| **后端** wenjuan-backend | `wenjuan-backend.git` ✅ | `wenjuan-backend.git` ✅ | 完全一致 ✅ |

### B端服务器未提交修改
1. `.env.production` — 已修改
2. `src/components/QuestionComponents/QuestionCheckbox/StatComponent.tsx` — 改成 ResponsiveContainer（= 本地 commit `7805ea9`）
3. `src/components/QuestionComponents/QuestionRadio/StatComponent.tsx` — 同上

**结论：服务器未提交修改实际上都已覆盖，可安全丢弃。**

### GitHub Actions 现状（已配置 ✅）
三个项目均有 `.github/workflows/deploy.yml`，用 `appleboy/ssh-action`：
1. SSH 到服务器
2. `git fetch --all && git reset --hard origin/main`
3. 写入环境变量（从 GitHub Secret）
4. `docker compose build --no-cache <service>`
5. `docker compose up -d <service>`

**Secrets 已在 GitHub 配置好**，无需额外设置。

## 实施步骤

### Step 1：统一 B端本地 remote

`wenjuan-fe` 已改名为 `wenjuan-frontend`，更新本地 remote：

```bash
cd F:\MyCodeWeb\wenjuan-frontend
git remote set-url origin https://github.com/Cabbage-2023/wenjuan-frontend.git
```

### Step 2：清理 gitignore

**B端** `.gitignore` 添加：
```
stats.html
叫爹.bat
.env.production
```

**C端** `.gitignore` 添加：
```
叫爹.bat
```

### Step 3：获取服务器配置到本地

从服务器拉取关键配置，存到 `docs/` 目录：
1. 宿主机 nginx：`/etc/nginx/sites-available/wenjuan` → `docs/host-nginx.conf`
2. docker-compose：`/home/ubuntu/wenjuan-platform/docker-compose.yml` → `docs/docker-compose.yml`
3. 确认服务器 Actions workflow 与本地一致

### Step 4：提交并推送

**B端**：
```bash
git add .gitignore docs/
git commit -m "chore: add docs, update gitignore [skip ci]"
git push origin main
```

**C端**：
```bash
git add .gitignore
git commit -m "chore: add 叫爹.bat to gitignore [skip ci]"
git push origin main
```

**后端** — 无需提交，已同步。

### Step 5：验证部署

推送后等待 GitHub Actions 完成，验证所有路由：
- `http://natsumeai.icu/` — B端首页
- `http://natsumeai.icu/question/edit/xxx` — B端编辑页 ✅ 不会404
- `http://natsumeai.icu/question/stat/xxx` — B端统计页 ✅ 不会404
- `http://natsumeai.icu/question/xxx` — C端答题页

### Step 6：收尾
1. 杀多余 natapp：`kill 221933`
2. 删除 `stats.html`（构建产物）
3. 验证 `.env.production` 已被 gitignore

## 涉及文件清单

| 文件 | 操作 |
|------|------|
| `F:\MyCodeWeb\wenjuan-frontend\.gitignore` | 添加 `stats.html`、`叫爹.bat`、`.env.production` |
| `F:\MyCodeWeb\wenjuan-frontend\docs/host-nginx.conf` | 新建（服务器拉取） |
| `F:\MyCodeWeb\wenjuan-frontend\docs/docker-compose.yml` | 新建（服务器拉取） |
| `F:\MyCodeWeb\wenjuan-client\.gitignore` | 添加 `叫爹.bat` |

## 验证方式

1. `git push` 后检查 GitHub Actions 运行状态
2. SSH 到服务器确认容器已重建：`docker compose ps`
3. 浏览器访问所有路由确认正常
